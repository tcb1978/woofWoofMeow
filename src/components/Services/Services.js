import React from 'react';
import Aux from '../../hoc/Aux';

export const Services = (props) => {
    return(
        <Aux>
            {props.children}
        </Aux>
    )
};

export default Services