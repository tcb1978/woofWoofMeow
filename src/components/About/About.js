import React from 'react';
import Aux from '../../hoc/Aux'

const About = (props) => {
    return (
        <Aux>
            AboutJs - an 'about' component
        </Aux>
    )
};

export default About;