import React, { Component } from 'react';
import Aux from '../../hoc/Aux';

class Jobs extends Component {
    render() {
        return (
            <Aux>Jobs</Aux>
        )
    }
};

export default Jobs;